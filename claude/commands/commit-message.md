Optional Additional Context: $ARGUMENTS

Review the staged changes and generate three (3) options of concise and descriptive commit messages that accurately summarizes the modifications made. If the user has no staged changes, try to suggest a commit message for unstaged changes instead.

The commit message should follow "conventional commit" guidelines, including a type (e.g., feat, fix, docs), a scope (optional), and a brief description of the changes.
Short commit messages are preferred.

DO NOT CREATE A COMMIT.
